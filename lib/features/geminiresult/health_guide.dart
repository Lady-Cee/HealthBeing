import 'package:flutter/material.dart';

class HealthGuidePage extends StatelessWidget {
  const HealthGuidePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
